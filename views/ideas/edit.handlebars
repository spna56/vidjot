{{#each errors}}
<div class="alert alert-danger">{{text}}</div>
{{else}}

{{/each}}

<div class="card card-body">
    <h3> Edit Video Idea</h3>
    <form action="/ideas/{{ideas.id}}?_method=PUT" method="POST">
    <label for="title">Title</label>
    <div class="form-group">
        <input class="form-control" type="text" name="ideas[title]" value="{{ideas.title}}" >
    </div>
    <label for="title">Details</label>
     <div class="form-group">
        <textarea class="form-control" type="text" name="ideas[details]"  >{{ideas.details}}</textarea>
    </div>
    <button type="submit" class="btn btn-primary">Submit</button>
        
    
    </form>
</div>
